# Impact Analysis

Read-only analysis. Modification, deletion, creation, movement, commit, and push are forbidden.

Analyze impact only:

- What files and functions may be affected
- What callers and downstream modules may be affected
- Whether runtime, queue, rules, GUI, SendOrder, Broker, or Kiwoom boundaries are involved
- What tests should be run before implementation
- What risks should block implementation

Report only. Do not perform implementation.

