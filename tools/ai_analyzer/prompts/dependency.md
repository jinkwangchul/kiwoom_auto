# Dependency Analysis

Read-only analysis. Modification, deletion, creation, movement, commit, and push are forbidden.

Analyze dependencies only:

- Imports
- Shared helper functions
- Data contracts
- Runtime files
- Queue files
- Rules files
- GUI dependencies
- Broker or Kiwoom dependencies
- Test dependencies

Report only. Do not perform implementation.

